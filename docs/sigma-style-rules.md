# Sigma-Style Rules

Each YAML rule includes `rule_id`, `title`, `description`, `log_source`, `detection_logic`, `severity`, `tactic`, `technique`, `false_positive_notes`, and `recommended_response`.

