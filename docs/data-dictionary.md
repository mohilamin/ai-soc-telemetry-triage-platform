# Data Dictionary

Key outputs include `security_alerts.csv`, `security_incidents.csv`, `analyst_queue.csv`, `blast_radius_report.csv`, `incident_timelines.csv`, `evidence_records.csv`, `response_recommendations.csv`, and SOC scorecards.

