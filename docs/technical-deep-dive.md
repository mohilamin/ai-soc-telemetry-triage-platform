# Technical Deep Dive

The platform creates deterministic telemetry and attack scenarios, applies local Sigma-style rules, creates alert evidence, deduplicates and correlates alerts into incidents, scores severity/confidence, generates timelines and runbooks, writes scorecards, and loads a DuckDB warehouse.

